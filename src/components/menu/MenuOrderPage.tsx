"use client";

import { MenuPOSPageClientProps } from "@/lib/type";
import MenuOrderHeader from "./MenuOrderHeader";

const MenuOrderPage = ({
  initialItems,
  relatedData,
}: MenuPOSPageClientProps) => {
  return <MenuOrderHeader />;
};

export default MenuOrderPage;
